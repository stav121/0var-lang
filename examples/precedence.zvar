main {
    int v$0 = 2;
    int v$1 = 3;
    int v$2 = 4;

    // Demonstrates that * has higher precedence than +
    // This calculates 2 + (3 * 4) = 14, not (2 + 3) * 4 = 20
    v$0 = v$0 + v$1 * v$2;
    print(v$0); // Output: 14
}